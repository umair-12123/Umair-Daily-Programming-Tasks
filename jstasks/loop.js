for(var tab = 1; tab<=10; tab++ ){
    var tableof = 8;
    console.log(tableof + " * " + tab + " = " + (tableof*tab));
}
var tab = 1;
while( tab<=10 ){
    var tableof = 8;
    console.log(tableof + " * " + tab + " = " + (tableof*tab));
    tab++
}
do{
    var tableof = 8;
    console.log(tableof + " * " + tab + " = " + (tableof*tab));
    tab++
}while( tab<=10 )
    // template letrals
    for(let num = 1; num<= 10; num ++){
        let tableof = 12;
        console.log(`${tableof} * ${num} = ${tableof * num}`)
    }
